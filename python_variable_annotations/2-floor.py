#!/usr/bin/python3
def floor(n: float) -> int:
    """Returns the floor of a number.

    Args:
        n: The number to round down.

    Returns:
        The floor of the number.
    """
    return int(n)
